#include <iostream>
#include <climits>
int main()
{
    // PATH_MAX is a constant that represents the maximum number of characters an absolute file path can contain on a POSIX system.
    std::cout << PATH_MAX << "\n";
}